//#include "renderer.h"

extern "C" {
	#include <wincurses/wincurses.h>
}

int main(int argc, char *argv[])
{
  (void)argc; (void)argv;
  ////user optionally passes in width and height, tries to fullscreen if args arent sent
  //Video vid(filename, width, height);
  //Renderer ren(vid);

  //while (vid.video_playing)
  //{ 
  //  ren.renderFrame(); //will hold until the frame is done playing
  //}
	return 0;
}
